import React from 'react';
import { Header, Footer } from '../components';

const Home = () => (
    <div>
        <Header />
        <h1>This is the homepage screen</h1>
        <Footer />
    </div>
);

export default Home;